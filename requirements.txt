# ══════════════════════════════════════════════════════════════
# RAG-Philosophy — Optimized Dependencies
# Chỉ giữ các thư viện trực tiếp sử dụng trong project.
# Các transitive dependencies (sub-deps) sẽ được pip tự cài.
# ══════════════════════════════════════════════════════════════

# ── Core LangChain Framework ──────────────────────────────────
langchain==0.3.25
langchain-core==0.3.84
langchain-community==0.3.24
langchain-text-splitters==0.3.11

# ── LangChain Integrations ────────────────────────────────────
langchain-google-genai==2.1.5          # Gemini LLM (Generator + Evaluator)
langchain-huggingface==0.2.0           # HuggingFaceEmbeddings (Harrier model)
langchain-qdrant==0.2.0                # Qdrant VectorStore adapter
langchain-cohere                       # Cohere Reranker integration

# ── Vector Database ───────────────────────────────────────────
qdrant-client==1.14.2                  # Qdrant persistent client

# ── PDF Processing ────────────────────────────────────────────
PyMuPDF==1.25.5                        # fitz — PDF parsing engine
pymupdf4llm==0.0.24                    # Markdown extraction from PDF

# ── NLP & Retrieval ───────────────────────────────────────────
rank_bm25                              # BM25 retriever backend
pyvi==0.1.1                            # Vietnamese tokenizer (BM25 preprocessing)

# ── ML Runtime ────────────────────────────────────────────────
torch==2.6.0+cu124                     # PyTorch (Harrier embedding model)
transformers==4.57.6                   # HuggingFace Transformers
sentence-transformers==4.1.0           # Sentence embedding framework
tokenizers==0.22.2                     # Fast tokenizer backend

# ── Data & Evaluation ────────────────────────────────────────
pandas==3.0.2                          # DataFrame — evaluation report
python-dotenv==1.1.0                   # .env file loader
pillow==12.2.0                         # Image processing (OCR downscale)

# ── Cohere SDK (transitive, pinned for stability) ─────────────
cohere                                 # Required by langchain-cohere

# ── Evaluation (Ragas & Metrics) ──────────────────────────────
ragas==0.4.3                           # LLM-as-a-judge framework (native)
datasets>=2.20.0                       # Required for ragas data format
nest-asyncio==1.6.0                    # Prevents event loop crashes